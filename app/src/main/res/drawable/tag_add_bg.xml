<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android" >
    <!-- 填充 -->
  <!--  <solid android:color="@color/gray_light" /> --> <!--定义填充的颜色值-->
    <!--  描边-->
    <stroke
        android:width="1sp"
        android:color="@color/gray_deep" />  <!--定义描边的宽度和描边的颜色值-->
    <!-- 圆角 -->
    <corners
        android:bottomLeftRadius="6dp"
        android:bottomRightRadius="6dp"
        android:topLeftRadius="6dp"
        android:topRightRadius="6dp" /> <!-- 设置四个角的半径 -->
    <!-- 间隔 -->
    <!--  <padding
          android:bottom="10dp"
          android:left="10dp"
          android:right="10dp"
          android:top="10dp" />--> <!-- 设置各个方向的间隔 -->

</shape>  